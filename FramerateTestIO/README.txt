This is a benchmark test adapted to Storyboard from:

http://www.craftymind.com/guimark/
 
The port was done using the HTML version and changing the JS to Lua

TODO: 
Currently there are open issues that prevent this from running correctly.

- Word wrap isn't re-calculated when control changes size
- needs to be ran from the head branch (post 1.1.1) since it needs gre.mstime()